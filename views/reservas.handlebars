<h1>Lista de Reservas3: </h1>
<!--
{{#each reservas}}
    <h1>Valida2 {{horario}}</h1>
    <h1>{{espacoId}}</h1>
    <h1>{{'espaco.capacidade'}}</h1>
    <h1>teste1 {{'espaco.nome_espaco'}}</h1>
    <h1>teste2 {{'usuario.nome'}}</h1>
    <p>teste3 {{reservado}}</p>
    <hr>
{{/each}}
!-->
<table border="1">
    <thead>
        <tr><th>Horario</th>
            <th>id_reserva</th>
            <th>id_espaco</th>
            <th>Status</th>
             <th>Espaço</th>
            <th>Capacidade</th>
            <th>Nome Reserva</th>
        </tr>
    </thead>
    <tbody>
        {{#each reservas}}
        <tr>            
            <th>{{horario}}</th>
            <th>{{id}}</th>
            <th>{{espacoId}}</th>
            <th>{{reservado}}</th>
             <th>{{'espaco.nome_espaco'}}</th>
            <th>{{'espaco.capacidade'}}</th>
            <th>{{'usuario.nome'}}</th>
            <th><form action="/reservar/{{id}}" method="post">
                <button type="submit">Reservar</button>
            </form></a></th>
            <th><form action="/cancelar/{{id}}" method="post">
            <!--    <a href="/cancelar/{{id}}"><button>Cancelar</button> !-->
                <button type="submit">Cancelar</button>
            </form></a></th>            
        </tr>
        {{/each}}
    </tbody>
</table>

